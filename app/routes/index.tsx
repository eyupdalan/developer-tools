import {withLayout} from '~/components/Layout';
import {Text} from "@chakra-ui/react";

function Index() {
    return (
        <Text>Tools for developers</Text>
    );
}

export default withLayout(Index);
